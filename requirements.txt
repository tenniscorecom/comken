; 社内BO環境（オフライン）では pip install -r は使えない。参照・記録用。
; --- 必須 ---
openpyxl
selenium
pywin32
requests
; --- 開発用 ---
; バージョンは .github/workflows/ci.yml と必ず揃える。
; ずれると formatter の挙動差で「手元は通るのに CI だけ落ちる」状態になる。
ruff==0.15.21
pytest==9.1.1
