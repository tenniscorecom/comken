"""comken/services/__init__.py — 社内の決まりに沿って部品を組み合わせた仕組み。

`toolbox/` の部品（Excel を読む・Salesforce と通信する）を組み合わせて、
**「社内では〜という決まりで〜する」でしか説明できない機能**を各プロジェクトへ提供する。

    from comken.services.salesforce_downloader import download_report

toolbox との境目は「説明のしかた」で決まる。

- **「〜を操作する／〜と通信する」で説明できる** → `toolbox/`
- **社内の管理表・管理番号・保存先の規約を知らないと説明できない** → ここ

**実行される単位（定期実行のバッチなど）はここに置かない。** それは個別プロジェクト
（このリポジトリの外）の仕事で、ここに置くのは**呼ばれる側**だけにする。
"""
