# comken — Claude Code 用エントリ

このリポジトリの指示は **`AGENTS.md` を唯一の正本（Source of Truth）** として管理しています。
Claude Code と Codex が同じ内容を読むための共存設計です。

以下の import で `AGENTS.md` の全文を取り込みます。
編集は必ず `AGENTS.md` 側で行ってください（この `CLAUDE.md` 本体は編集不要）。

@AGENTS.md
