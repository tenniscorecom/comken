# コードリーディングガイド（レビュー用）

このライブラリ（comken）をレビュー・読解する人向けの地図。
「どこから読めばいいか」「どのファイルも同じ形になっている」ことを先に押さえると速く読める。

- 何のライブラリか: CSV・Excel の読み書きやブラウザ操作などを、
  非エンジニアでも使い方を真似できる形でまとめた業務自動化の共通部品集
- 規約: [CONVENTIONS.md](../CONVENTIONS.md)（命名・型ヒント・例外・ロギング等はすべてここに準拠）
- 使い方の説明: [README.md](../README.md)（利用者向け）。本書はコードを読む人向け

---

## 全体地図

| パッケージ | 役割 | 主役クラス・関数 | 外部依存 |
|---|---|---|---|
| `exceptions/` | 1つの失敗＝1クラスの例外体系（`ComkenError` 基底） | — | なし |
| `constants/` | 公開定数クラス（1クラス1ファイル） | `Encoding`, `Color`, `FileFormat`, `SortBy` | なし |
| `utils/files/` | ファイル探索・操作・圧縮・パス取得・命名 | `FileFinder`, `DateNameBuilder`, `move_file` | なし |
| `utils/clock.py` | PC のローカルタイムゾーン付き現在時刻・今日の日付 | `now`, `today` | なし |
| `runtime.py` | dry-run / debug のグローバルスイッチ | `dry_run` ほか | なし |
| `config.py` | config.ini 読み込み（`config.SECTION.KEY` アクセス） | `Config` | なし |
| `utils/` | 汎用部品（差分比較・テキスト・リトライ・待機・日時） | `diff_rows`, `retry`, `now` | なし |
| `csv/` | CSV 読み書き（文字コード自動判定） | `CsvReader`, `CsvWriter` | なし |
| `excel/` | Excel 読み書き（openpyxl。Excel 本体不要） | `ExcelReader`, `ExcelWriter`, `Sheet` | openpyxl |
| `access/` | Access のマクロ・VBA 実行と大量データの CSV 出力 | `AccessDatabase` | pywin32・Microsoft Access |
| `outlook/` | Classic Outlook の受信メール読み取り・下書き作成 | `Outlook`, `MailMessage` | pywin32・Classic Outlook |
| `windows/` | Excel COM（数式・マクロ・パスワード保存）、ウィンドウ・レジストリ | `ExcelComHandler` | pywin32 |
| `browser/` | ブラウザ自動操作（Page Object Model の土台） | `Browsers`, `BrowserSession`, `Page` | selenium |

### 依存の向き

```
constants/ / exceptions/ / runtime.py / utils/ / utils/files/  ← 土台
        ▲
csv/ / excel/ / windows/ / browser/ / config.py                 ← 機能
```

- 機能パッケージ同士は原則独立。例外は1つだけ:
  `excel/`（openpyxl）が数式計算・マクロ実行時に `windows/`（COM）へ
  **遅延 import でフォールバック**する（pywin32 がない環境でも excel/ 単体は動く）

---

## 読む順番（おすすめ）

1. **[CONVENTIONS.md](../CONVENTIONS.md)** — 全ファイル共通のルール。これを先に読むと以降が速い
2. **`exceptions/`** — カテゴリ別ファイルと個別例外。エラーメッセージの方針
   （非エンジニアが読んで対処できる文面）もここで分かる
3. **`constants/` → `utils/clock.py` → `utils/files/`** — 依存の少ない基盤
4. **`runtime.py` → `config.py`** — 実行モードと設定

`config.py` は config.ini の初回読み込み完了時に、障害調査用の comken バージョンを
INFO ログへ1回だけ記録する。
5. **`csv/base.py` → `csv/reader.py` / `csv/writer.py` → `excel/base.py` → `excel/reader.py` / `excel/writer.py` → `excel/sheet.py`** — 一番よく使われる部分。
   ここで「docstring の書き方」「定数クラス」「with 文」のパターンをつかむ
6. **`windows/handler.py` → `browser/`** — OS・外部アプリ依存の処理

## どのファイルも同じ形

全モジュールが同じ並び順で書かれている（詳細: CONVENTIONS.md「モジュール内の並び順」）:

```
1. モジュール docstring   ← そのファイルの役割と使い方（コピペで動く例）
2. import
3. logger・型変数
4. 定数・定数クラス
5. 主役の公開クラス       ← モジュール名が指すもの
6. その他の公開クラス・公開関数
7. 内部ヘルパー（_ 付き）  ← 必ず最後
```

つまり**ファイル先頭の docstring と主役クラスの docstring だけ読めば、そのモジュールの
全体像と使い方が分かる**。細部（内部ヘルパー）は必要なときだけ下へ読み進めればよい。

---

## 設計上の約束事（レビュー観点の要約）

| 観点 | 方針 | 確認場所 |
|---|---|---|
| 例外 | 1つの失敗＝1つの個別例外。`ComkenError` 基底。メッセージは個別例外の `__init__` で組み立てる | `exceptions/` |
| リソース解放 | ファイル・COM・WebDriver はすべて `with` 対応（`__enter__`/`__exit__`） | 各 handler |
| ログ | `print` 禁止。`logging` のみ。機密の値はログに出さない | 全ファイル |
| dry-run | `with dry_run():` の範囲だけ書き込み系操作をログ出力だけにできる | `runtime.py` |
| 型ヒント | 全関数に引数・戻り値の型。`Any` 不使用 | 全ファイル |
| 静的チェック | Ruff（line-length 100, py310。正確な有効ルールは設定を正本とする）全ファイルパス | `pyproject.toml` |
| 公開 API | 利用側が必要な名前だけ `__all__` で公開し、基底クラスは公開しない | 各 `__init__.py` |

ログの設定（出力先・フォーマット・レベル）は社内の共通ライブラリ側で行う。
comken と利用プロジェクトは `logging.getLogger(__name__)` を使うだけでよい。

## テスト

```
python -m pytest tests -q    # テスト一式（環境依存のケースは skip）
```

- 単体テストは pytest。一時ファイルは `tmp_path` フィクスチャで完結し、外部環境不要
- **対象外**: Excel / Access COM の実動作（各アプリ本体が必要）とブラウザ実機。
  これらは利用プロジェクト側の初回実行時に動作確認する運用

## 想定問答

**Q. Excel の処理が openpyxl と COM の二本立てなのはなぜ？**
通常の読み書きは openpyxl（Excel 本体不要・高速・サーバーでも動く）。
既存数式の計算結果の読み取り・マクロ実行・パスワード付き保存だけは Excel 本体の機能が必要なので
COM（`ExcelComHandler`）を使う。使い分けは各クラスの docstring 冒頭に明記してある。

