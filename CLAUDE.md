# comken — AI 向けの入口

現職（派遣先）の業務自動化で使う Python 共通ライブラリ。フォルダ名は `original_libs`、
パッケージ名は `comken`。**非エンジニアが保守する前提**なので、短く賢いコードより
半年後に読めるコードを取る。

## 最初に読むもの

| 目的 | ファイル |
|---|---|
| コーディング規約（命名・型ヒント・例外・ロギング） | `CONVENTIONS.md` |
| **なぜその設計にしたか**（設計判断の一次情報） | `仕様書.md` |
| 何が用意されているか（やりたいこと→クラス名） | `docs/機能カタログ.md`（早見表・37行） |

## 大きいファイル — 開く前に grep すること

丸ごと読むとトークンを大量に消費する。**必要な節だけ読む。**

| ファイル | 大きさ | 中身 |
|---|---|---|
| `docs/API.md` | **4,900行** | 全公開 API の署名と docstring。**生成物**（`python export_for_chat.py` で再生成）。手で編集しない |
| `README.md` | **1,400行** | 利用者向け。モジュールごとのコード例が主体 |
| `CONVENTIONS.md` | 700行 | 規約 |
| `tests/` | 5,900行 | テスト。変更するとき以外は読まなくてよい |

## ドキュメントを増やす前に

**同じことを2箇所に書かない。** API の説明（引数・戻り値・例外）は **docstring が一次情報**で、
`docs/API.md` はそこから自動生成する。手書きのメソッド一覧表を作らないこと
（2026-08-13 に `docs/機能カタログ.md` を 577行→37行に縮めたのはこのため）。

| 情報 | 置き場所 |
|---|---|
| API の使い方（引数・戻り値・例外） | **docstring**（→ `docs/API.md` へ自動生成） |
| 何を使えばいいか | `docs/機能カタログ.md` の早見表 |
| 動く例 | `examples/`（`tests/test_examples.py` が実際に実行する） |
| なぜそう設計したか | `仕様書.md` / `docs/Salesforce.md` |
| エラーが出たときの対処 | `ERRORS.md` |

## 環境の制約（設計判断の背景）

- **社内 BO 環境はオフラインで pip が使えない。** 依存は最小限にし、標準ライブラリを優先する
- 配布は**共有サーバー直接参照**。`実行.bat` が `COMKEN_ROOT` を `PYTHONPATH` に足す。
  VS Code の補完を効かせるため `.vscode/settings.json` の
  `python.analysis.extraPaths` にも同じ場所が要る（`new_project.py` が両方へ自動で書き込む）
- 認証情報は **DPAPI**（Windows ユーザー + PC に紐付く）。**別 PC では復号できない**
- **このリポジトリは公開しているので社内ライブラリの実名を書かない。**
  `example_libs.v0000` の仮名にしてある。実名を書き戻さないこと

## 変更したら

```
python -m pytest          # 全通過が前提
python -m ruff check
python export_for_chat.py # 公開 API を変えたら docs/API.md を再生成する
```

`ruff format --check` は既存7ファイルが未整形のまま残っている（既存の負債。
触るときに直す方針で、まとめて整形はしない）。
