# 引き継ぎメモ（2026-08-19 時点）

comken の Phase 4 見直しを、Claude Code（対話セッション）が途中まで進めた。
**次に触るエージェント（MiniMax M3 を想定）は、まずこのファイルを読むこと。**

## いまの状態（この時点の HEAD）

| 項目 | 値 |
|---|---|
| `python -m pytest` | **1357 passed / 25 skipped** |
| `npx --yes pyright@latest comken/` | **0 errors** |
| `python -m ruff check .` | **0 errors** |

**この 3 つの数字を、作業のたびに自分で走らせて確かめること。**
数字が下がったら、その作業で壊している。報告の緑は根拠にならない。

## 直近でやったこと（新しい順）

1. `b1aa390` 検査結果の型を共通化（`CheckResult` / `DoctorResult` の重複解消）、
   `check_facade()` を件数比較から名前比較へ
2. `7c5b53a` `wait_for_file` でフォルダ不在と「ファイルがまだ来ない」を区別
3. `c6eaec6` 使われていない `Result` 型を削除
4. `74efe62` `run_id` の ContextVar の説明を実測に合わせて修正
5. `9836ca2` Salesforce の直接実装を `direct/` へ集約、テンプレートの
   `認証情報の登録.bat` を最小化

## 決まっていること（勝手に変えない）

- **v1.0.0 で `wait` クラスを廃止し、4関数に統一済み。**
  `wait_seconds` / `wait_until` / `wait_for_file` / `wait_until_stable`
  を用途に合わせて使う
- **`core/files/` の責務分割（archive / atomic / finder / naming / ops）は維持する**
- **`comken/__init__.py` のファサードは増やさない。** 新しい公開 API は
  `comken/core/__init__.py` まで
- **Salesforce のクラス名は `SalesforceBase` のまま。** パッケージ名 `direct` が
  既に「直接実装」を表しており、クラス名を変えると利用プロジェクトの継承が全部壊れる
- **`bat` は CP932 / CRLF で保存する。** UTF-8 で保存すると cmd.exe で日本語が化け、
  `tests/test_batch_files.py` が落ちる
- **例外は増やさない方針。** 組み込み例外（`FileNotFoundError` /
  `NotADirectoryError` など）で意味が合うならそれを使う

## 既知の残エラー

**無し。** `pytest` / `pyright` / `ruff` の 3 つとも通る状態になっている。
**この状態を下げないこと。** 作業のたびに 3 つとも自分で走らせて確かめる。

## 次にやるべきこと

**いま積んである作業は無い。** P2 まで片付いている（下の「片付けたもの」参照）。
次は、実際に使う場面で困ったことを起点にする。**「綺麗にできそう」という理由だけで
既存 API を変えないこと。**

## 片付けたもの（蒸し返さない）

- `check/__init__.py` にロジック本体（約 300 行）が入っていた件 → `check/runner.py`
  へ移し、`__init__.py` は再エクスポートだけにした（`doctor` と同じ形）
- `check_pyright()` の docstring が「`tests/test_pyright_clean.py` と同じ判定ロジック」と
  言い過ぎていた件 → 「件数を読み取る判定は同じだが、pyright の探し方は違う」に修正。
  テスト側は開発機専用なので `npx --yes pyright@latest` でよいが、
  **check 側は BO / オフラインで打つコマンドなので npm を見に行ってはいけない**
- `tests/test_doctor.py` の F811（同名テストの二重定義）→ 古いほうを削除。
  `_resolve_salesforce_deps()` が 2 値返しだった頃のまま取り残されており、
  **実行されていれば失敗する死にコード**だった（後の定義に上書きされて隠れていた）
- `tests/test_holidays.py` の E501 9 件 → `_computed_calendar(year)` に括り出した。
  折り返しを増やすより、何年を見ているのかが読める形を選んだ

## 却下した案（蒸し返さない）

- **汎用の `Result` 型を置く**（`ok` / `warn` / `empty` / `skip`）。
  一度入れたが `c6eaec6` で削除した。comken 内に利用箇所が 1 つも無く、
  `success=False` を作る経路も存在せず、`Generic[T]` はファサード経由で
  機能していなかった（`ok(data=123)` が `Result[Unknown]` になる）。
  **実際に統一したい対象が見えてから、その形に合わせて入れ直す**
- **Salesforce の `wrap/` パッケージ**（社内ライブラリ向けの Protocol + stub）。
  社内ライブラリの API 仕様が未確定のまま `NotImplementedError` を投げる
  stub を 3 つ置いても、当たる保証がない。設計メモは Brain 側
  （`Projects/comken-設計メモ_salesforce-wrap.md`）に残してある

## 作業のルール

- **コミットしない。** 差分は作業ツリーに置いたまま残し、人間が `git diff` で
  確認してからコミットする
- **`git add -A` を使わない。** 別セッションが同じリポジトリを触っている前提で、
  自分が変更したファイルだけを明示的にステージする
- **テストが落ちたら、テストのほうを緩めない。** 検査対象から除外するリストを
  作る、アサーションを弱めるといった直し方をしない。落ちた理由を実装側で直すか、
  直せないなら報告して止まる
- **日本語ファイルは UTF-8 で保存する**（`bat` を除く）。
  cp932 で保存すると docstring が壊れ、Python の構文エラーになる。
  **一度壊れた日本語は元に戻せない**
- **ファイルを移動・改名したら、旧パスの文字列を `grep` して残存参照を洗う。**
  過去に `docs/` のコード例が `ImportError` になったまま、
  テストが存在しないモジュール名を検査して**アサーションが無意味に真**に
  なったまま残っていたことがある
